# Alternative configuration for Static Site on Render
# Use this if you want to use Render's static site hosting instead of Node.js

services:
  - type: web
    name: samecoin-static
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

